{{- if .Values.rbacHelpers.enable }}
# Permissions for end users to edit deceptionalertsinks
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: koney-deceptionalertsink-editor-role
rules:
- apiGroups:
  - research.dynatrace.com
  resources:
  - deceptionalertsinks
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - research.dynatrace.com
  resources:
  - deceptionalertsinks/status
  verbs:
  - get
{{- end }}
